"use client";

import { FC } from "react";
import { DefaultProps } from "../page.d";
// import { LanguageSlug, getLanguageLocalStorage } from "languages-module";

const LanguageSelector: FC<DefaultProps> = ({}) => {
  // const languageSlug = getLanguageLocalStorage();

  // const [isOpen, setIsOpen] = useState(false);

  // const handleLanguageChange = (languageSlug: LanguageSlug) => {
  //   setLanguageSlug(languageSlug);
  //   setIsOpen(false);
  // };

  return <div>LanguageSelector</div>;
};

export default LanguageSelector;
